#include "main.h"

/**
 * _strlen - computes the length of the passed string
 * @s: string to be computed
 * Return: 0 if completed successfully
 */

int _strlen(char *s)
{
	int length;

	for (length = 0; s[length] != '\0'; length++)
	{
	}
	return (length);
}
