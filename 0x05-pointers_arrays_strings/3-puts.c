#include "main.h"

/**
 * _puts - prints the passed string to stdout
 * @str: passed string
 *
 */

void _puts(char *str)
{
	int length, j;

	for (lenth = 0; s[length] != '\0'; length++)
	{
	}
	for (j = 0; j < length; j++)
		_putchar(s[j]);
	_putchar('\n');
}
