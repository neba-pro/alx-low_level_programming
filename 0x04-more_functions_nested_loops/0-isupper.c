#include "main.h"

/**
 * _isupper - checks if uppercase
 * @c: parameter passed from the user
 * Return: 0 if completed successfully
 */

int int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		return (1);
	else
		return (0);
	return (0);
}
