#include "main.h"

/**
 * _isdigit - checks if it is digit
 * @c: parameter passed from user
 * Return: 0 or 1
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
		return (1);
	else
		return (0);
	return (0);
}
